`kualta.testnet`


```sh
near contract call-function as-read-only kualta.testnet get_greeting
```

```
> How would you like to pass the function arguments? text-args - Arbitrary text arguments
> Enter the arguments to this function: 
> What is the name of the network? testnet
> Сhoose block for view: now - View properties in the final block
--------------
No logs
--------------
Result:
"Hello"
--------------
```